import { SectionWrapper } from "@/components/ui/SectionWrapper";

export function AboutOverviewSection() {
  return (
    <SectionWrapper title="About Us" description="Who we are and how we work.">
      <p className="text-black/80">
        Primquisites combines product strategy, design, and engineering to help
        teams ship confidently.
      </p>
    </SectionWrapper>
  );
}
